TELEGRAM_BOT_TOKEN=
GROQ_API_KEY=
CHAT_ID=
# Leave WEBHOOK_URL empty for local polling mode.
# On Render set to your service URL, e.g. https://my-bot.onrender.com
WEBHOOK_URL=
# Optional: override sqlite path (use /var/data/... on Render persistent disk)
DB_PATH=
